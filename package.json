{
  "name": "vital-veda-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:dev_tbsc": "NODE_ENV=dev hardhat run --network tbsc",
    "deploy:dev_apothem": "NODE_ENV=dev hardhat run --network apothem",
    "verify:dev_tbsc": "NODE_ENV=dev hardhat verify --network tbsc",
    "verify:dev_apothem": "NODE_ENV=dev hardhat verify --network apothem"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.12",
    "@openzeppelin/contracts": "^5.2.0",
    "hardhat": "^2.22.18"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5",
    "hardhat-contract-sizer": "^2.10.0"
  }
}
